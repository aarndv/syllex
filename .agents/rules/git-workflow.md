# Git Commit & Push Workflow

- **Automatic Commit & Push**: Stage (`git add`), commit (`git commit -m "..."`), and push (`git push`) all workspace changes whenever a modification is made, unless the user explicitly requests not to push.
- **Commit Messages**: Write clear, descriptive commit messages summarizing the changes made.
